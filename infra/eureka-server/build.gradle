version '1.0-SNAPSHOT'

dependencies {
    implementation libpacks.spring_comon
    implementation libpacks.cloud_common
    implementation libpacks.eureka_server
}

def fileName = 'eureka-server'
bootJar{
    mainClass = 'com.epam.EurekaServerApplication'
    archiveName "${fileName}.jar"
}
