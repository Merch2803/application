version '1.0-SNAPSHOT'

dependencies {
    implementation libpacks.spring_comon
    implementation libpacks.cloud_config_server
}

def fileName = 'config-server'
bootJar{
    mainClass = 'com.epam.ConfigServerApplication'
    archiveName "${fileName}.jar"
}